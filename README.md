# 📦 Simple Photography store (Php + MySQLi + Storage)

A lightweight PHP project demonstrating basic CRUD operations using **MySQLi** and a simple `storage/` system for handling uploaded files or persistent data.

---

